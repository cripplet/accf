syntax = "proto3";

package accf_protos;

import "ball.proto";
import "locale.proto";

message Q2DBox {
  // UNIX timestamp.
  // TOOD(cripplet): Replace with google.protobuf.Timestamp.
  int64 timestamp = 1;

  Locale size = 2;
  repeated Ball balls = 3;
}
